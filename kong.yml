_format_version: "3.0"
_transform: true

services:
  - name: order-api
    url: http://order_api:5000
    routes:
      - name: order-api-route
        paths:
          - /api/orders
        methods:
          - POST
        strip_path: false
